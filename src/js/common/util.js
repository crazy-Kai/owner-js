define(function(require,exports,module){
	'use strict';
	//依赖
	var Promise = require('./promise');
	module.exports = {
		formateDate:function(formate,temp){
			var timeRag = /^yyyy(.MM)?(.dd)?(.HH)?(.mm)?(.ss)?$/;
		}	
	}
})